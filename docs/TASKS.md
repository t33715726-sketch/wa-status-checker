# משימות

## הושלם

- [x] גרסה 1 — סריקה, ניתוח, ייצוא vcf מהשרת
- [x] תיקון BOM בקובץ ה-vcf (`12e910c`)
- [x] שחזור אנשי הקשר שנדרסו — דרך "ביטול שינויים" באנשי הקשר של גוגל.
      אומת: חיפוש "סטטוס" לא מחזיר אף רשומה, מונה הכפילויות ירד מ-98 ל-8
- [x] **גרסה 2 — ההצלבה והייצוא עברו לדפדפן**
  - [x] `public/contacts.js` — קריאת vCard/CSV, נרמול מספרים, בניית vcf עם בדיקה כפולה
  - [x] `public/index.html` — שלב "העלה את אנשי הקשר שלך" לפני החיבור
  - [x] `src/waSession.js` — שולח `phone` מלא ללקוח, מפסיק להחזיק תוצאות
  - [x] `src/server.js` — הוסרה `/api/export`
  - [x] `public/app.js` — מסך `viewContacts`, חסימת המשך בלי ספר טלפונים,
        סינון התוצאות, ייצוא מקומי בלי קריאת רשת
  - [x] `src/vcf.js` נמחק — אין יותר בנייה בשרת
  - [x] קיפול שורות ל-75 בתים בבונה שבדפדפן
  - [x] 20 בדיקות עוברות, כולל הרגרסיה שגרמה לתקלה
  - [x] בדיקת ממשק מקצה לקצה על הדפדפן

- [x] **מסלול "מי שמר אותי" — כל מה שאפשר לבנות בלי גישה לספק**
  - [x] `src/providers/meProvider.js` — מתאם עם OTP, אימות, משיכה וביטול טוקן
  - [x] `src/meSession.js` — סשן שמדבר אותה שפה כמו סשן וואטסאפ
  - [x] `me:start` / `me:verify` ו-`GET /api/sources` בשרת
  - [x] מסך בחירת מקור, מסכי טלפון וקוד
  - [x] מיזוג שני המקורות לרשימה אחת ולקובץ אחד, בלי כפילויות
  - [x] בחירה לפי מפתח טלפון ולא לפי אינדקס (אינדקסים מתנגשים בין מקורות)
  - [x] כבוי כברירת מחדל, עם סימון ברור בממשק
  - [x] נבדק מקצה לקצה מול שירות מדומה: OTP, קוד שגוי, מיזוג, ייצוא

## פתוח

- [ ] **לפתוח את החסם של Me** — בלי גישה מורשית המסלול מושבת.
      כל השאר בנוי ומחכה. נקודת החיבור: `ME_API_BASE` ב-`.env`
- [ ] **בדיקה עם ייצוא אנשי קשר אמיתי של מוישי** — לוודא שהקובץ מכיל אפס
      מספרים שכבר קיימים בייצוא. זה התנאי לחידוש השירות
- [ ] לחדש את השירות ב-Render (Settings → `Resume Web Service`)
- [ ] לוודא שהשמות בטלפון חזרו אחרי השחזור:
      סמסונג → הגדרות → חשבונות וגיבוי → ניהול חשבונות → Google → סנכרון החשבון
      → ⋮ → סנכרן עכשיו. ואז וואטסאפ → צ'אט חדש → ⋮ → רענון
- [ ] להחליט על Me — לשלוח את הפנייה (טיוטה ב-`docs/HANDOVER.md` סעיף 8)
      או לוותר על המסלול
- [ ] README מתאר פריסה ל-Railway, אבל השירות יושב על Render. לעדכן
